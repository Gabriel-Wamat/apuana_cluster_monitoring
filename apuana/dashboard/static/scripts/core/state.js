const hist = {run:[], pnd:[], total:[]};
const MAX_HIST = 30;
const SPARKLINE_COLOR = 'var(--spark)';
const sparkRegistry = new Map();
let sparkFrame = null;
let _last = {};
let transferState = {user:'', host:'slurm-client1.cin.ufpe.br', home:'', current:'', selectedKind:'', selectedPath:''};
let downloadState = {localFolder:''};
let uploadState = {files:[]};
let transferFeedbackState = {type:'idle', mode:'', title:'Ready', message:'Select a remote item or choose a local folder to start.', details:'', command:'', canRetry:false};
let transferBrowseTimer = null;
let lastTransferBrowseKey = '';
let transferLayoutRaf = 0;
let uploadRemoteSuggestTimer = null;
let lastUploadRemoteSuggestKey = '';
let downloadRemoteSuggestTimer = null;
let lastDownloadRemoteSuggestKey = '';
let remoteExplorerState = {path:'', home:'', period:'all', viewMode:'grid', viewPreferenceLoaded:false, items:[], deletePath:'', editPath:'', loaded:false, history:[], future:[]};
const remoteExplorerCache = new Map();
let codeState = {projects:[], project:'', path:'', parent:'', query:'', items:[], entries:[], tree:[], openFiles:[], activePath:'', loading:false, loaded:false, createKind:'', deletePath:'', deleteKind:''};
let codeTreeActivated = false;
let codeFolderState = {path:'', items:[], loading:false, error:''};
let codeSearchTimer = null;
let codeRequestSeq = 0;
let codeFolderRequestSeq = 0;
let codeFolderMenuOpen = false;
const codeExpandedPaths = new Set();
const codeListCache = new Map();
const codeTreeCache = new Map();
const codeFolderCache = new Map();
const codeFileCache = new Map();
const codeViewModeByPath = new Map();
const codeDraftCache = new Map();
const codeDirtyPaths = new Set();
const codeSavingPaths = new Set();
let codeTerminalState = {open:false, id:'', output:'', status:'idle', host:'', login:'', polling:false, pollTimer:null, starting:false, startPromise:null, pendingInput:''};
let gpuState = {jobId:'', response:null, raw:'', rawVisible:false, loadingId:''};
let jobState = {raw:'', rawVisible:false, inspectedId:'', loadingId:''};
let logState = {items:[], folders:[], loaded:false, folder:'', parent:'', home:'', mode:'folders', error:'', history:[]};
let logViewState = {raw:{out:'', err:''}};
const AUTH_STORAGE_KEY = 'apuana.sshAuth';
let sshAuth = {login:'', token:'', remember:false, loggedIn:false};
let waitingForFirstData = false;
let _loaderShownAt = 0;
let _loaderHideTimer = null;
let clusterDataReady = false;
let clusterBootNoticeShown = false;
const LOADER_MIN_MS = 2200;
